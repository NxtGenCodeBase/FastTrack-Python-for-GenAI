# Lists: Mutable (Changeable) - Core for data sequences
ai_models = ["GPT-4", "Llama-3", "Claude-3"]
ai_models.append("Gemini")

# Tuples: Immutable (Fixed) - Good for constant settings
hyperparameters = (0.001, 32, "Adam")  # lr, batch_size, optimizer

print(f"Models to study: {ai_models}")
print(f"Fixed training config: {hyperparameters}")
