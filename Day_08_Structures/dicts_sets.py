# Dictionaries: Key-Value pairs (The backbone of JSON/API data)
model_config = {
    "name": "GPT-4o",
    "parameters": "Unknown",
    "multimodal": True
}

# Sets: Unique items only (Great for cleaning duplicates)
tags = {"NLP", "Vision", "NLP", "Robotics"}
print(f"Unique AI Tags: {tags}")  # Returns only 3 items
print(f"Model Name: {model_config['name']}")
